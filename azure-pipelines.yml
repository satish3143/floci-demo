trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:

- script: echo "Floci Azure Pipeline Working"
  displayName: Step 1

- script: |
    sudo apt-get update
    sudo apt-get install -y awscli
  displayName: Install AWS CLI

- script: aws --version
  displayName: Verify AWS CLI